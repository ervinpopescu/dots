#!/bin/bash

# SSID="$(iwgetid -r)"
# short="$(ip route get 8.8.8.8 | cut -d" " -f5 | head -1)"
# short="${short:0:2}"
# if [[ "$short" = "wl" ]]
# then
# 	printf "%s %s" "" "$SSID"
# elif [[ "$short" = "en" ]]
# then
# 	printf "%s %s" "" "$SSID"
# fi

netspeed.sh